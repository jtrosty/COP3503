#include "Draw.h"
#include "utils.h"

void Draw::drawRectangle(int x0, int y0, int x1, int y1, UINT32 color, RenderBuffer& buffer) {
    for (int i = x0; i < x1; i++) {
        for (int j = y0; j < y1; j++) {

        }
    }
}